# ── Betriebssystem / Editor ──────────────────────────────────────────────────
.DS_Store
.claude/

# ── Node / Build (VeloroutenCheckWeb) ────────────────────────────────────────
node_modules/
dist/
*.local
.env
.env.*

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc

# ── Generierte Auswertungs-Outputs (regenerierbar via tools/verify_06.py) ─────
# 06_verifikation.md wird als nachvollziehbarer Ergebnis-Snapshot versioniert;
# .json (Rohdump) und .html (generierte Ansicht) bleiben aussen vor.
06_verifikation.json
06_visualisierung.html

# ── Interne Arbeitsnotiz (kein Methodik-Inhalt) ──────────────────────────────
docs/ux-verbesserungen-plan.md

# ── Grosse / Dritt-Rohdaten: lokal halten, NICHT versionieren ────────────────
# (Bezugsquellen sind im README dokumentiert; die App nutzt nur die gebündelten
#  Snapshots unter VeloroutenCheckWeb/public/.)
Fahrplan/
Grundlagen/
Notizen/
FixMyCity_Daten/
Geoinformation Stadt Bern/
OBS_Daten.json
